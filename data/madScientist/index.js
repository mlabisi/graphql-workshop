const { shopOwners } = require('./shopOwners');
const { shops } = require('./shops');
const { addresses } = require('./addresses');
const { menuItems } = require('./menuItems');
const { hoursItems } = require('./hoursItems');

module.exports = {
  shopOwners,
  shops,
  addresses,
  menuItems,
  hoursItems,
};